{
	"name": "santan-monorepo",
	"private": true,
	"packageManager": "npm@11.18.0",
	"license": "MIT",
	"scripts": {
		"dev": "echo '🚀 Starting development servers...' && turbo run dev",
		"dev:stream": "echo '📺 Starting development servers in stream mode...' && turbo run dev --ui=stream",
		"dev:tui": "echo '🖥️  Starting development servers in TUI mode...' && turbo run dev --ui=tui",
		"build": "echo '🏗️  Building all packages...' && turbo run build",
		"lint": "echo '🔍 Linting code...' && biome lint .",
		"lint:fix": "echo '🔧 Fixing lint issues...' && biome lint --write .",
		"type-check": "echo '✅ Type checking...' && turbo run type-check",
		"format": "echo '✨ Formatting code...' && biome format --write .",
		"format:check": "echo '🔍 Checking formatting...' && biome format .",
		"check": "echo '🔍 Running all checks...' && biome check .",
		"check:fix": "echo '🔧 Fixing all issues...' && biome check --write .",
		"clean": "echo '🧹 Cleaning build artifacts...' && turbo run clean",
		"test": "playwright test",
		"test:e2e": "playwright test --ui",
		"test:e2e:report": "playwright show-report",
		"vitest": "npm run vitest --workspace=@santan/frontend"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.4",
		"@playwright/test": "^1.61.1",
		"tsx": "^4.23.1",
		"turbo": "^2.10.5"
	},
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"engines": {
		"node": ">=24"
	},
	"dependencies": {
		"@sanity/vision": "^6.5.0",
		"diff": "^9.0.0",
		"sanity": "^6.5.0"
	},
	"allowScripts": {
		"@parcel/watcher@2.5.6": true,
		"esbuild@0.28.1": true
	}
}
